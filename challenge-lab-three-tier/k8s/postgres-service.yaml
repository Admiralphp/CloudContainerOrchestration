apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: three-tier-app
  labels:
    app: postgres
    tier: database
spec:
  type: ClusterIP
  selector:
    app: postgres
    tier: database
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
